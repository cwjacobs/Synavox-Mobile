<Page class="page" navigatingTo="onNavigatingTo" loaded="onLoaded" xmlns="http://schemas.nativescript.org/tns.xsd">
	<ActionBar class="action-bar">
		<!-- 
        Use the NavigationButton as a side-drawer button in Android
        because ActionItems are shown on the right side of the ActionBar
        -->
		<NavigationButton ios:visibility="collapsed" icon="res://menu" tap="onDrawerButtonTap"></NavigationButton>
		<ActionItem icon="res://navigation/menu" android:visibility="collapsed" tap="onDrawerButtonTap" ios.position="left"></ActionItem>
		<Label class="action-bar-title" text="Home Pharmacist"></Label>
	</ActionBar>
	<!-- <GridLayout class="page-content"> -->
	<Label class="page-icon fa" text="&#xf129;"></Label>
	<!-- <Label class="page-placeholder" text="Page content goes here"></Label> -->
	<StackLayout>
		<image src="~/images/home.jpg" width="100%" height="auto"></image>
		<Label class="list title" text="My Medicines"></Label>
		<ListView class="list" items="{{ myMedicineList }}" itemTap="onItemTap">
			<ListView.itemTemplate>
				<GridLayout row="*" col="*,*">
					<Label class="fa" style="text-align: right; margin-right: 10px;" text="&#xf29e;" row="0" col="0"/>
					<Label style="padding: 10px" text="{{ medicineName }}" row="0" col="1"/>
				</GridLayout>
			</ListView.itemTemplate>
		</ListView>
		<GridLayout class="button-row" rows="*" columns="*,*,*">
			<Button class="button btn-primary" text="English" row="0" col="0" onTap="onEnglishTap" />
			<Button class="button test-button fa" text="Stop &#xf29e;" onTap="onStopTap" row="0" col="1" />
			<Button class="button btn-primary" text="Spanish" row="0" col="2" onTap="onSpanishTap" />
		</GridLayout>
	</StackLayout>
	<!-- </GridLayout> -->
</Page>