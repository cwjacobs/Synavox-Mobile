<Page class="page" navigatingTo="onNavigatingTo" loaded="onLoaded" xmlns="http://schemas.nativescript.org/tns.xsd">
	<ActionBar class="action-bar">
		<!-- 
        Use the NavigationButton as a side-drawer button in Android
        because ActionItems are shown on the right side of the ActionBar
        -->
		<NavigationButton ios:visibility="collapsed" icon="res://menu" tap="onDrawerButtonTap"></NavigationButton>
		<!-- 
        Use the ActionItem for IOS with position set to left. Using the
        NavigationButton as a side-drawer button in iOS is not possible,
        because its function is to always navigate back in the application.
        -->
		<ActionItem icon="res://navigation/menu" android:visibility="collapsed" tap="onDrawerButtonTap" ios.position="left"></ActionItem>
		<Label class="action-bar-title" text="Browse"></Label>
	</ActionBar>
	<StackLayout>
		<image src="~/images/browse.jpg" width="100%" height="auto"></image>
		<Label class="list title" text="{{ i18NMedicineListTitle }}"></Label>
		<!-- <GridLayout class="page-content"> -->
		<ListView class="list" items="{{ myMedicineList }}" itemTap="onItemTap">
			<ListView.itemTemplate>
				<GridLayout rows="auto" cols="auto,auto">
					<!-- <Label class="row-text" text="{{ medicineName }}"  row="0" col="0" colSpan="3" /> -->
					<Label class="alert" text="My" row="0" col="0" />
					<Label class="primary" text="Web" row="0" col="1" />
					<!-- <Label class="secondary" text="Your" row="0" col="2" /> -->
				</GridLayout>
			</ListView.itemTemplate>
		</ListView>
		<!-- </GridLayout> -->
	</StackLayout>
</Page>